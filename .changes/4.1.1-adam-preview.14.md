## What's Changed

- normalize managed solution `source.url` comparisons so Terraform ignores query-string and fragment churn while still detecting real source transport changes
- add unit coverage for URI query/fragment churn and legacy localhost source transitions
