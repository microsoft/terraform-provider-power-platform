## What's Changed

- normalize managed solution `source.url` comparisons so Terraform ignores rotating `token` query values but still detects real source transport changes
- add unit coverage for token-only URL churn and legacy localhost source transitions
